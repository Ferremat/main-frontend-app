{{- if .Values.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.app.container.name }}-config
  namespace: {{ .Release.Namespace }}
data:
  NUXT_PUBLIC_API_URL: {{ .Values.env.NUXT_PUBLIC_API_URL | quote }}
  NITRO_PORT: "3000"
  NITRO_HOST: "0.0.0.0"
{{- end }}
